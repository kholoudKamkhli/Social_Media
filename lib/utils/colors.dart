import 'package:flutter/material.dart';

const mobileBackgroundColor = Colors.white; // Changed to white for light theme
const webBackgroundColor = Colors.white; // Changed to white for light theme
const mobileSearchColor = Color.fromRGBO(240, 240, 240, 1); // Light grey for search bar
const blueColor = Color.fromRGBO(0, 149, 246, 1);
const primaryColor = Colors.black; // Changed to black for better visibility on light background
const secondaryColor = Colors.grey; // Grey color for secondary elements
